-- SPDX-License-Identifier: LGPL-3.0

-- Define the package name
local pkgname = "dcsex"
local _G      = _G

-- Prevent duplicate loading
if _G[pkgname] ~= nil then
	error(string.format("package: `%s` already exists, skipping...",
		pkgname))
end

-- Define the module table with submodules
local libs = {
	_VERSION     = "%VERSION%",
	_DESCRIPTION = string.format("%s: DCS Extensions library.", pkgname),
	_COPYRIGHT   = "Copyright (c) 2019-2022,2024-2025 Jonathan Toppins",

	-- Submodules
	ai           = require(pkgname..".ai"),
	algorithms   = require(pkgname..".algorithms"),
	check        = require(pkgname..".check"),
	class        = require(pkgname..".class"),
	containers   = require(pkgname..".containers"),
	converter    = require(pkgname..".converter"),
	debug        = require(pkgname..".debug"),
	enum         = require(pkgname..".enum"),
	env          = require(pkgname..".env"),
	io           = require(pkgname..".io"),
	json         = require(pkgname..".json"),
	math         = require(pkgname..".math"),
	string       = require(pkgname..".string"),
	setters      = require(pkgname..".setters"),
	table        = require(pkgname..".table"),
	tacan        = require(pkgname..".tacan"),
	Timer        = require(pkgname..".Timer"),
	ui           = require(pkgname..".ui"),
	utils        = require(pkgname..".utils"),
	utility      = require(pkgname..".utility"),
	vector       = require(pkgname..".vector"),
	world        = require(pkgname..".world"),
}

-- Register the module in the global environment
_G[pkgname] = libs

-- Return the module table for use in requiring scripts
return libs
